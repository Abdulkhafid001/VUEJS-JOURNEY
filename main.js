//create vue since imported from CDN
const app = Vue.createApp({
    // create an6d in6itialize a data ob5ject
    data(){
        // the data also return6s an6 ob5ject to b5e passed to the con6strun6ctor
        // create a product attrib5ute
        product: "Socks";
    }
})

let test = ""